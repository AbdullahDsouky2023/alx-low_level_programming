#include "main.h"
#include "ctype.h"
/**
* _isupper- checks is it is uppercase4 or not
*
* @c : the intger want to be checked
*
* Return: 0
*/

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
