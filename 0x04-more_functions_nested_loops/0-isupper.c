#include "main.h"
#include "ctype.h"
/**
* isupper- checks is it is uppercase4 or not
*
* Return: 0
*/

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0); 
}
