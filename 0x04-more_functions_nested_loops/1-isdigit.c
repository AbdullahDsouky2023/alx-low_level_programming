#include "main.h"
#include <ctype.h>
/**
* _isdigit- checks is it is uppercase4 or not
*
* @c :the the digit to be checked
*
* Return: 0
*/

int _isdigit(int c)
{
	if(isdigit(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
