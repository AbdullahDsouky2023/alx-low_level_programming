#include "main.h"
#include <string.h>
/**
 * _strlen - function
 * @s :the char to know it;s length
 * Description :' fef '
 * Return: Always strlen(s).
*/

int _strlen(char *s)
{
	return (strlen(s));
}
