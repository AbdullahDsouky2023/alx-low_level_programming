#include "main.h"
#include <stdio.h>
/**
 * _puts -  function to print word
 * @str : the word to be printed
 * Description: 'function desc'
 * Return: Always 0.
 */

void _puts(char *str)
{
	puts(str);
}
